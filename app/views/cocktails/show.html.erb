<%= render 'cocktails/banner', cocktail: @cocktail %>
<div class="container">
  <div class="row">
    <div class="col-6">
      <%=  render 'cocktails/dose_list', cocktail: @cocktail %>
    </div>
    <div class="col 6">
      <%= render 'cocktails/dose_form', cocktail: @cocktail, dose: @dose %>
    </div>
  </div>
  <div class="row flex-justify-center">
    <%= render 'cocktails/review_list', cocktail: @cocktail %>
    <%= render 'cocktails/review_form', cocktail: @cocktail, review: @review %>
  </div>
</div>
